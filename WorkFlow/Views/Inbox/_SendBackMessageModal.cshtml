@{
    ViewBag.Title = "Comments for revision:";
}
<div style="background-color: white;">
    @using (Ajax.BeginForm("SendBack", "Inbox", null, new AjaxOptions()
    {
        HttpMethod = "post",
        InsertionMode = InsertionMode.Replace,
        UpdateTargetId = "div_content",
        LoadingElementId = "div_progress"
    }, new { id = "sendback_comments_form" }))
    {
        @Html.Hidden("flowCaseId")
        <div class="form-group">
            @Html.TextArea("comments", new { style = "width:570px;max-width:570px;height:150px;max-height:150px;", data_val = "true",data_val_required="*" })
        </div>
        <script type="text/javascript">
            $(function () {
                $("#sendback_comments_form").removeData("validator").removeData("unobtrusiveValidation");
                $.validator.unobtrusive.parse($("#sendback_comments_form"));
            });
        </script>
    }
</div>